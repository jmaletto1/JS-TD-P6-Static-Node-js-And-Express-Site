extends layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9

        //- Project Title
        h1= `${projectData.project_name}`

        //- Project Description
        p.lead.text-light= `${projectData.description}`

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies

        //- Use of Pug's "each" iterator to display each project technology.
        ul
          each val in projectData.technologies
            li #{val}

        //- Links to Live Project and GitHub Repo, displayed using Template Literals
        a.button.expanded(href=`${projectData.live_link}` target="_blank") Live Demo
        a.button.expanded(href=`${projectData.github_link}` target="_blank") GitHub Repo
    
    //- Images displayed using template literals and pug's "each" iteration method.
    each val, index in projectData.image_urls
      img.image(src=`${projectData.image_urls[index]}`)